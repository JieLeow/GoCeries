11/19 Thursday

- Added user_ in the users table 
CREATE TABLE `users` (
  `user_id` int(11) NOT NULL,
  `user_loginname` varchar(255) NOT NULL,
  `user_password` varchar(255) NOT NULL,
  `user_name` varchar(255) NOT NULL,
  `user_email` varchar(255) NOT NULL



- Only pushed the new file for login and register pages.
- Let me know when you want me to push the modified mainPageHeader, header, account , catergory-allProducts , and main page to integrate with the login and register page.

- Have'nt found a way to use funtions.php, so I am still using the db_conn.php
